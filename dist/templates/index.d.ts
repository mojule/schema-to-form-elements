import { Templates } from '../types';
export declare const ServerFormTemplates: (document: Document) => Templates;
export declare const ClientFormTemplates: (document: Document, Event: any) => Templates;
